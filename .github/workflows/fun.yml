name: TDS GA1 Action

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: 24f2008529@ds.study.iitm.ac.in
        run: echo "Hello, world!"
